# Pinned to what is verified working on this machine (see bench.md).
# torch is CUDA-specific: install it from the appropriate index for your GPU.

# --- core inference ---
torch==2.13.0+cu126
diffusers==0.39.0
transformers==5.15.0
accelerate==1.14.0
torchao==0.18.0
safetensors==0.8.0
sentencepiece==0.2.2
pillow==12.3.0

# --- prompt enhancement / SVG authoring ---
ollama==0.6.2

# --- service + MCP ---
fastapi==0.141.1
uvicorn==0.52.3
mcp==2.0.0
httpx==0.28.1

# --- SVG toolchain ---
defusedxml==0.7.1
resvg-py==0.4.0
vtracer==0.6.15

# --- validation ---
pydantic==2.13.4
